# actor_critic.py
ActorCritic
假设：

- 观测维度 `mlp_input_dim_a = 11`
- 隐藏层 `actor_hidden_dims = [256, 256, 128]`
- 动作维度 `num_actions = 3`
- 激活函数 `activation = nn.ReLU()`

## 最终搭出来的网络结构（一层一层写死）

1. Linear(11 → 256)
2. ReLU
3. Linear(256 → 256)
4. ReLU
5. Linear(256 → 128)
6. ReLU
7. Linear(128 → 3)