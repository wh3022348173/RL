ppo.py
process_env_step(self, rewards, dones, infos)
超时自举
- 环境可能因为时间限制而终止（并非真正的失败）
- 使用当前值函数估计未来回报，避免低估状态价值

GAE (Generalized Advantage Estimation) 计算：

优势函数 A(s,a) 衡量动作相对于平均水平的优劣：
```
A(s,a) = Q(s,a) - V(s)
```
PPO 代理损失
- 如果动作好（A > 0），鼓励提高概率，但不超过旧策略的 1.2 倍
- 如果动作差（A < 0），鼓励降低概率，但不超过旧策略的 0.8 倍